<div id="comment-made" class="border border-dark my-2">
    <div id="comment-details">
        <p>On {{{format_date date}}}, <a href=/user/{{user.id}}>{{user.username}}</a> wrote...</p>
    </div>
    <div id="comment-body">
        <p>{{comment}}</p>
    </div>
</div>
