  <div class="col-md-6">
    <a href=../blog/{{id}}>
    <div class="blog-title">
      <h3>{{title}}</h3>
    </div>
    <div class="blog-details">
      <h4>{{slugline}}</h4>
      </div>
    </a>
    <div>
      <p>Written by: <a href=/user/{{user.id}}><span>{{user.username}}</span></a> on <span>{{format_date date}}</span>. <span>{{comments.length}} comments</span></p>
    </div>
  </div>